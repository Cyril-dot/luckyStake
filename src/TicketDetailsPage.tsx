import { useEffect, useState } from 'react';
import { api, formatKickoff, isFinishedMatch, isLiveStatus, isMatchLive, liveClock, type MatchRow } from './api';

type AnyRecord = Record<string, any>;
function n(...values: unknown[]) { for (const value of values) { const parsed = Number(value); if (Number.isFinite(parsed)) return parsed; } return 0; }
function s(...values: unknown[]) { const value = values.find(item => item !== undefined && item !== null && String(item).trim()); return value == null ? '' : String(value); }
function money(value: number) { return `GH₵${value.toLocaleString('en-GH', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`; }
function list(payload: unknown): AnyRecord { if (payload && typeof payload === 'object' && 'data' in (payload as AnyRecord)) return (payload as AnyRecord).data; return (payload || {}) as AnyRecord; }
function selections(raw: AnyRecord): AnyRecord[] { const value = raw.selections ?? raw.picks ?? raw.legs ?? raw.betSelections ?? []; return Array.isArray(value) ? value : []; }
function verifyCode(id: string) { const raw = id.replace(/[^A-Z0-9]/gi, '').toUpperCase(); return `GH${raw.slice(0, 4)}${raw.slice(-6)}`; }

const SAMPLE_BET: AnyRecord = { id: 'sample-ls-2026', status: 'WON', stake: 50, totalOdds: 4.86, potentialReturn: 243, placedAt: '2026-09-25T13:22:00Z', selections: [
  { id: 'sample-leg-1', matchId: 'sample-match-1', homeTeam: 'Hearts of Oak', awayTeam: 'Asante Kotoko', market: 'Match result', selection: 'Home', oddsLocked: 2.15, result: 'Won', scoreHome: 2, scoreAway: 1 },
  { id: 'sample-leg-2', matchId: 'sample-match-2', homeTeam: 'Barcelona', awayTeam: 'Valencia', market: 'Over/Under', selection: 'Over 2.5', oddsLocked: 2.26, result: 'Won', scoreHome: 3, scoreAway: 1 },
] };
const SAMPLE_OPEN_BET: AnyRecord = { id: 'sample-open-2026', status: 'PENDING', stake: 25, totalOdds: 3.2, potentialReturn: 80, placedAt: '2026-09-25T13:30:00Z', selections: [{ id: 'sample-open-leg', matchId: 'sample-open-match', homeTeam: 'Accra Lions', awayTeam: 'Medeama SC', market: 'Match result', selection: 'Draw', oddsLocked: 3.2 }] };

const SAMPLE_WIN_AMOUNT = 75990;

const SAMPLE_MATCHES: Record<string, MatchRow> = {
  'sample-match-1': { id: 'sample-match-1', homeTeam: 'Hearts of Oak', awayTeam: 'Asante Kotoko', status: 'FINISHED', kickoffAt: '2026-09-25T09:00:00Z', scoreHome: 2, scoreAway: 1 },
  'sample-match-2': { id: 'sample-match-2', homeTeam: 'Barcelona', awayTeam: 'Valencia', status: 'FINISHED', kickoffAt: '2026-09-25T10:00:00Z', scoreHome: 3, scoreAway: 1 },
};

export default function TicketDetailsPage({ id }: { id: string }) {
  const [bet, setBet] = useState<AnyRecord | null>(null);
  const [matches, setMatches] = useState<Record<string, MatchRow>>({});
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [cashingOut, setCashingOut] = useState(false);
  const [celebrate, setCelebrate] = useState(false);
  const referencePreview = import.meta.env.DEV && new URLSearchParams(window.location.search).get('preview') === 'winning';

  useEffect(() => { document.body.classList.add('ticket-details-route'); return () => { document.body.classList.remove('ticket-details-route'); }; }, []);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError('');
    const previewMode = new URLSearchParams(window.location.search).get('preview') || '';
    const samplePreview = import.meta.env.DEV && ['sample', 'open', 'winning'].includes(previewMode);
    if (samplePreview) { const pendingPreview = previewMode === 'open'; setBet(pendingPreview ? SAMPLE_OPEN_BET : SAMPLE_BET); setMatches(SAMPLE_MATCHES); setLoading(false); return () => { cancelled = true; }; }
    api<unknown>('GET', `/api/bets/${encodeURIComponent(id)}`).then(async payload => {
      if (cancelled) return;
      const nextBet = list(payload);
      setBet(nextBet);
      const ids = selections(nextBet).map(leg => s(leg.matchId, leg.match_id, leg.eventId, leg.match?.id)).filter(Boolean);
      const rows = await Promise.all(ids.map(async matchId => {
        try { return [matchId, await api<MatchRow>('GET', `/api/public/football/matches/${encodeURIComponent(matchId)}`)] as const; }
        catch { try { return [matchId, await api<MatchRow>('GET', `/api/matches/${encodeURIComponent(matchId)}`)] as const; } catch { return null; } }
      }));
      if (!cancelled) setMatches(Object.fromEntries(rows.filter(Boolean) as Array<readonly [string, MatchRow]>));
    }).catch(e => { if (!cancelled) setError(e instanceof Error ? e.message : 'Ticket could not be loaded.'); }).finally(() => { if (!cancelled) setLoading(false); });
    return () => { cancelled = true; };
  }, [id]);

  if (loading) return <main className="ls-ticket-page"><TicketStyles /><div className="ls-ticket-loading"><i /> Loading ticket details…</div></main>;
  if (error || !bet) return <main className="ls-ticket-page"><TicketStyles /><div className="ls-ticket-error"><span className="material-symbols-rounded">error</span><h1>Ticket unavailable</h1><p>{error || 'This ticket could not be found.'}</p><a href="/slip">Back to my bets</a></div></main>;

  const status = s(bet.status, bet.state, bet.result, 'PENDING').toUpperCase();
  const won = ['WON', 'WIN', 'PAID', 'CASHED_OUT', 'CASHOUT'].includes(status);
  const lost = ['LOST', 'LOSS', 'VOID', 'CANCELLED', 'CANCELED'].includes(status);
  const open = !won && !lost;
  const stake = n(bet.stake, bet.amount, bet.stakeAmount);
  const odds = n(bet.totalOdds, bet.odds, bet.combinedOdds);
  const payout = n(bet.potentialReturn, bet.potentialWin, bet.payout, bet.returnAmount, stake * odds);
  const legs = selections(bet);
  if (open) return <main className="ls-ticket-page"><TicketStyles /><div className="ls-ticket-unavailable"><span className="material-symbols-rounded">schedule</span><h1>Ticket details unavailable</h1><p>Ticket details are available after every match has finished and the bet has been settled.</p><a href="/slip">Back to open bets</a></div></main>;

  async function cashout() {
    setCashingOut(true);
    try { await api('POST', `/api/bets/${encodeURIComponent(id)}/cashout`); const refreshed = await api<unknown>('GET', `/api/bets/${encodeURIComponent(id)}`); setBet(list(refreshed)); }
    catch (e) { setError(e instanceof Error ? e.message : 'Cashout could not be completed.'); }
    finally { setCashingOut(false); }
  }

  return <main className="ls-ticket-page"><TicketStyles /><div className="ls-ticket-shell">
    <header className="ls-ticket-header"><a className="ls-ticket-header-btn" href="/slip" aria-label="Back to bets"><span className="material-symbols-rounded">arrow_back</span></a><h1>Ticket details</h1><span className="ls-ticket-header-spacer" aria-hidden="true" /></header>
    <section className={`ls-ticket-summary ${won ? 'won' : lost ? 'lost' : 'pending'}`}>
      <div className="ls-ticket-summary-top"><span>Ticket ID: {id.slice(0, 10).toUpperCase()}</span><span>{s(bet.placedAt, bet.createdAt, bet.created_at, 'Recently placed')}</span></div>
      <div className="ls-ticket-summary-row"><strong>{legs.length > 1 ? 'Multiple' : 'Singles'}</strong><b className="ls-ticket-summary-status">{won ? 'Won' : lost ? 'Lost' : 'Pending'}</b></div>
      <div className="ls-ticket-stat"><span>{won || status === 'CASHED_OUT' ? 'Total return' : 'Potential return'}</span><b className={won ? 'return-won' : ''}>{money(payout)}</b></div>
      <div className="ls-ticket-stat"><span>Total stake</span><b>{money(stake)}</b></div>
      <div className="ls-ticket-stat"><span>Total odds</span><b>{odds.toFixed(2)}×</b></div>
      <div className="ls-ticket-verify">Verify code: <strong>{verifyCode(id)}</strong></div>
      {won && <div className="ls-ticket-win-banner"><div className="ls-ticket-win-message"><span className="ls-ticket-win-trophy"><img src="/superbet-victory-trophy.png" alt="LuckyStake winning trophy" /></span><span>Congratulations!<br /><b>You won this ticket.</b></span></div><button type="button" onClick={() => setCelebrate(true)}>Show off</button></div>}
    </section>
    <section className="ls-ticket-legs">{legs.length ? legs.map((leg, index) => {
      const match = matches[s(leg.matchId, leg.match_id, leg.eventId, leg.match?.id)];
      const home = s(leg.homeTeam, leg.home_team, leg.homeName, match?.homeTeam, 'Home');
      const away = s(leg.awayTeam, leg.away_team, leg.awayName, match?.awayTeam, 'Away');
      const kickoff = s(leg.kickoffAt, leg.kickoff_at, leg.startTime, leg.scheduledAt, match?.kickoffAt);
      const scoreHome = leg.scoreHome ?? leg.score_home ?? match?.scoreHome;
      const scoreAway = leg.scoreAway ?? leg.score_away ?? match?.scoreAway;
      const legStatus = s(leg.status, leg.result, leg.matchStatus, match?.status);
      const ended = Boolean((match && isFinishedMatch(match)) || ['FINISHED', 'FULL_TIME', 'FT', 'ENDED', 'COMPLETED', 'COMPLETE'].includes(legStatus.toUpperCase()));
      const live = !ended && (match ? isMatchLive(match) : isLiveStatus(legStatus));
      const legWon = ['WON', 'WIN', 'PAID'].includes(legStatus.toUpperCase()) || won;
      const legOdds = n(leg.oddsLocked, leg.submittedOdds, leg.selectionOdds, leg.odds, leg.odd, leg.price);
      return <article className={`ls-ticket-leg-card ${legWon ? 'leg-won' : ''}`} key={`${s(leg.id, index)}`}>
        <div className="ls-ticket-leg-top">{legWon && <span className="material-symbols-rounded ls-leg-check">check_circle</span>}<div><strong>{home} <em>v</em> {away}</strong><small>Game ID: {s(leg.matchId, leg.match_id, 'Not available').slice(0, 10)}</small>{kickoff && <span className={live ? 'ls-kickoff live' : 'ls-kickoff'}>{ended ? 'Match ended' : live ? `Live ${match ? liveClock(match) : 'now'}` : `Starts ${formatKickoff(kickoff)}`}</span>}</div></div>
        <div className="ls-ticket-pick-box"><div><span>Pick</span><b>{s(leg.selection, leg.outcome, leg.pick, 'Selection')}{legWon && <span className="ls-pick-check">✓</span>}</b></div><div><span>Odds</span><b>{legOdds > 0 ? legOdds.toFixed(2) : 'Not available'}</b></div><div><span>Market</span><b>{s(leg.market, leg.marketName, leg.betType, 'Match result')}</b></div><div><span>Score</span><b>{scoreHome != null && scoreAway != null ? `${ended ? 'FT ' : ''}${scoreHome}-${scoreAway}` : live ? 'Live score pending' : 'Score not recorded'}</b></div><div><span>Outcome</span><b>{s(leg.result, ended ? 'Result recorded' : live ? 'Live' : 'Pending')}</b></div></div>
      </article>;
    }) : <div className="ls-ticket-no-legs">Selection details are not available for this ticket.</div>}</section>
    {open && <button className="ls-ticket-cashout" type="button" onClick={cashout} disabled={cashingOut}>{cashingOut ? 'Cashing out…' : `Cash out ${money(payout)}`}</button>}
    {error && <p className="ls-ticket-detail-error">{error}</p>}
  </div>{celebrate && <div className="ls-reference-win" role="dialog" aria-modal="true" aria-label="Winning ticket celebration"><ReferenceWinningStyles /><div className="ls-reference-win-confetti" aria-hidden="true">{Array.from({ length: 28 }).map((_, i) => <i key={i} style={{ left: `${(i * 37) % 100}%`, top: `${12 + ((i * 29) % 82)}%`, transform: `rotate(${(i * 47) % 180}deg)`, animationDelay: `${(i % 8) * .12}s` }} />)}</div><button className="ls-reference-win-close" type="button" onClick={() => setCelebrate(false)} aria-label="Close winning celebration"><span className="material-symbols-rounded">close</span></button><div className="ls-reference-win-content"><h2>YOU WON</h2><span>AMOUNT WON</span><strong>{money(payout)}</strong><img className="ls-reference-win-trophy" src="/superbet-victory-trophy.png" alt="LuckyStake winning trophy" /><button className="ls-reference-win-details" type="button" onClick={() => setCelebrate(false)}><span className="material-symbols-rounded">receipt_long</span> View ticket details</button><div className="ls-reference-win-actions"><button type="button" onClick={() => setCelebrate(false)}>Close</button><button type="button" onClick={() => setCelebrate(false)}><span className="material-symbols-rounded">share</span> Show Off</button></div></div></div>}{referencePreview && <ReferenceWinningPreview />}</main>;
}

function ReferenceWinningPreview() { return <div className="ls-reference-win" role="dialog" aria-modal="true" aria-label="Winning ticket preview"><ReferenceWinningStyles /><div className="ls-reference-win-confetti" aria-hidden="true">{Array.from({ length: 28 }).map((_, i) => <i key={i} style={{ left: `${(i * 37) % 100}%`, top: `${12 + ((i * 29) % 82)}%`, transform: `rotate(${(i * 47) % 180}deg)`, animationDelay: `${(i % 8) * .12}s` }} />)}</div><button className="ls-reference-win-close" type="button" onClick={() => window.history.back()} aria-label="Close preview"><span className="material-symbols-rounded">close</span></button><div className="ls-reference-win-content"><h2>YOU WON</h2><span>AMOUNT WON</span><strong>GHS {SAMPLE_WIN_AMOUNT.toLocaleString('en-GH', { minimumFractionDigits: 2 })}</strong><img className="ls-reference-win-trophy" src="/superbet-victory-trophy.png" alt="Winning trophy" /><button className="ls-reference-win-details" type="button"><span className="material-symbols-rounded">receipt_long</span> View ticket details</button><div className="ls-reference-win-actions"><button type="button">Close</button><button type="button"><span className="material-symbols-rounded">share</span> Show Off</button></div></div></div>};

function ReferenceWinningStyles() { return <style>{`
.ls-reference-win{position:fixed;inset:0;z-index:999;display:flex;align-items:stretch;justify-content:center;overflow:hidden;background:rgba(13,9,27,.9);backdrop-filter:blur(9px)}.ls-reference-win-content{position:relative;z-index:2;width:100%;min-height:100%;display:flex;flex-direction:column;align-items:center;padding:172px 24px 38px;background:rgba(21,16,39,.42)}.ls-reference-win-content>h2,.ls-reference-win-content>span,.ls-reference-win-content>strong,.ls-reference-win-content>.ls-reference-win-trophy,.ls-reference-win-content>.ls-reference-win-details,.ls-reference-win-content>.ls-reference-win-actions{max-width:420px;width:100%;text-align:center;margin-left:auto;margin-right:auto}.ls-reference-win-content h2{margin:0 auto;font:900 34px/1 'Barlow Condensed',sans-serif;letter-spacing:.015em;background:linear-gradient(180deg,#f1ff9a,#d6ee46);-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent}.ls-reference-win-content>span{margin:9px auto 0;color:#d6ee46;font:800 10px/1 'Rajdhani',sans-serif;letter-spacing:.18em}.ls-reference-win-content>strong{margin:7px auto 0;color:#fff;font:900 24px/1.1 'DM Sans',sans-serif}.ls-reference-win-trophy{width:min(235px,68vw);height:245px;object-fit:contain;margin:15px auto 22px;filter:drop-shadow(0 12px 15px rgba(0,0,0,.48))}.ls-reference-win-close{position:absolute;right:18px;top:15px;z-index:4;display:grid;place-items:center;width:32px;height:32px;border:1px solid rgba(160,207,255,.7);border-radius:50%;background:#21182e;color:#d6ee46;cursor:pointer}.ls-reference-win-close .material-symbols-rounded{font-size:20px}.ls-reference-win-details{display:flex;align-items:center;justify-content:center;gap:7px;width:100%;min-height:44px;border:0;border-radius:9px;background:#d6ee46;color:#211b29;font:800 13px Rajdhani;box-shadow:0 7px 16px rgba(0,99,220,.27);cursor:pointer}.ls-reference-win-details .material-symbols-rounded,.ls-reference-win-actions .material-symbols-rounded{font-size:15px}.ls-reference-win-actions{display:flex;gap:9px;width:100%;margin-top:10px}.ls-reference-win-actions button{flex:1;min-height:40px;border-radius:9px;border:1px solid #5a456b;background:rgba(33,24,46,.82);color:#fff;font:800 12px Rajdhani;cursor:pointer}.ls-reference-win-actions button+button{border-color:#d6ee46;background:linear-gradient(135deg,#3b2a56,#21182e)}.ls-reference-win-confetti{position:absolute;inset:0;z-index:3;pointer-events:none;overflow:hidden}.ls-reference-win-confetti i{position:absolute;width:5px;height:10px;background:#56a9ff;opacity:.8;animation:lsRefConfetti 3.8s linear infinite}.ls-reference-win-confetti i:nth-child(3n){background:#e5e9ef}.ls-reference-win-confetti i:nth-child(4n){background:#1e57bf}@keyframes lsRefConfetti{0%{opacity:0;transform:translateY(-30px) rotate(0deg)}15%{opacity:.9}100%{opacity:0;transform:translateY(110px) rotate(280deg)}}@media(max-width:380px){.ls-reference-win-content{padding-left:28px;padding-right:28px;padding-top:145px}.ls-reference-win-content h2{font-size:30px}.ls-reference-win-trophy{height:220px;margin-bottom:18px}}
`}</style>; }

function TicketStyles() { return <style>{` .ls-ticket-page{min-height:calc(100vh - 170px);padding:0 14px 80px;background:linear-gradient(145deg,#171220,#2a2038);color:#fff;font-family:'Rajdhani',Manrope,system-ui,sans-serif}.ls-ticket-shell{display:contents}.ls-ticket-header{display:flex;align-items:center;justify-content:space-between;gap:14px;min-height:64px;margin:0 0 14px;padding:0;background:transparent!important;border:0!important;box-shadow:none!important}.ls-ticket-header h1{margin:0;font:700 23px/1 'Rajdhani',sans-serif;letter-spacing:.02em}.ls-ticket-header-actions{display:flex;gap:8px}.ls-ticket-header-btn{display:grid;place-items:center;width:42px;height:42px;border:1px solid #5a456b;border-radius:9px;background:#21182e;color:#d4c7dc;text-decoration:none}.ls-ticket-header-btn:hover{border-color:#d6ee46;color:#d6ee46}.ls-ticket-header-btn .material-symbols-rounded{font-size:19px}.ls-ticket-summary{overflow:hidden;border:0;border-radius:0;background:transparent;box-shadow:none}.ls-ticket-summary-top{display:flex;justify-content:space-between;gap:12px;padding:14px 0 8px;color:#b9aec5;font-size:11px}.ls-ticket-summary-row{display:flex;align-items:center;justify-content:space-between;padding:4px 0 12px}.ls-ticket-summary-row>strong{font-size:24px}.ls-ticket-summary-status{padding:6px 10px;border-radius:99px;background:#332342;color:#d6ee46;font-size:12px;text-transform:uppercase}.ls-ticket-summary.won .ls-ticket-summary-status{background:#d6ee46;color:#211b29}.ls-ticket-summary.lost .ls-ticket-summary-status{background:#4a2734;color:#ffb4be}.ls-ticket-stat{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:8px 0;color:#c7bbd1;font-size:13px;border-top:1px solid rgba(255,255,255,.08)}.ls-ticket-stat b{color:#fff;font-size:15px;text-align:right}.ls-ticket-stat b.return-won{color:#57e5a2;font-size:21px}.ls-ticket-verify{padding:11px 0;color:#c2b6ca;font-size:11px;background:rgba(20,10,48,.3);border-top:1px solid rgba(255,255,255,.1)}.ls-ticket-verify strong{color:#fff;letter-spacing:.08em}.ls-ticket-win-banner{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:12px 0;background:#17131d;border-top:1px solid rgba(214,238,70,.25)}.ls-ticket-win-message{display:flex;align-items:center;gap:10px;font-size:12px;line-height:1.25}.ls-ticket-win-message b{color:#d6ee46}.ls-ticket-win-trophy{display:grid;place-items:center;width:68px;height:68px;flex:0 0 68px}.ls-ticket-win-trophy img{width:68px;height:68px;object-fit:contain;filter:drop-shadow(0 5px 7px rgba(0,0,0,.42))}.ls-ticket-win-banner button,.ls-trophy-modal-card>button{border:0;border-radius:8px;padding:10px 16px;margin-right:8px;background:#d6ee46;color:#211b29;font:800 12px Rajdhani;text-transform:uppercase;cursor:pointer}.ls-ticket-legs{display:flex;flex-direction:column;gap:12px;margin:14px 0;border-top:0}.ls-ticket-leg-card{padding:16px;border:1px solid #5a456b;border-radius:12px;background:linear-gradient(145deg,rgba(42,32,56,.88),rgba(24,18,35,.9));color:#fff;box-shadow:0 8px 18px rgba(7,3,15,.2)}.ls-ticket-leg-top{display:flex;align-items:flex-start;gap:10px;margin-bottom:13px;padding-bottom:11px;border-bottom:1px solid rgba(214,238,70,.18)}.ls-ticket-leg-top>div{display:flex;flex-direction:column;gap:4px;min-width:0}.ls-ticket-leg-top strong{font:700 17px/1.15 'Rajdhani',sans-serif;color:#fff}.ls-ticket-leg-top em{color:#b9aec5;font-style:normal;margin:0 4px}.ls-ticket-leg-top small{color:#b9aec5;font-size:10px}.ls-ticket-leg-card:nth-child(even){border-color:#6a527d}.ls-ticket-leg-card:nth-child(odd){border-color:#536a46}.ls-leg-check{color:#29a968;font-size:22px;margin-top:1px}.ls-kickoff{color:#b77c09;font-size:11px;font-weight:800}.ls-kickoff.live{color:#159454}.ls-ticket-pick-box{position:relative;display:grid;grid-template-columns:1.35fr .65fr 1fr .7fr 1fr;gap:0;margin-top:4px;padding:25px 6px 8px;border:1px solid #80649a;border-radius:10px;background:linear-gradient(135deg,rgba(55,39,72,.88),rgba(31,23,45,.92));box-shadow:inset 0 1px 0 rgba(214,238,70,.1)}.ls-ticket-pick-box:before{content:'GAME DETAILS';position:absolute;left:13px;top:8px;color:#d6ee46;font-size:9px;font-weight:900;letter-spacing:.12em}.ls-ticket-pick-box>div{padding:3px 10px;border-right:1px solid rgba(128,100,154,.45)}.ls-ticket-pick-box>div:last-child{border-right:0}.ls-ticket-pick-box>div{display:flex;flex-direction:column;gap:4px;min-width:0}.ls-ticket-pick-box span{color:#b9aec5;font-size:10px;text-transform:uppercase;letter-spacing:.05em}.ls-ticket-pick-box b{color:#fff;font-size:13px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ls-ticket-pick-box>div:nth-child(2) b{color:#d6ee46}.ls-pick-check{color:#159454!important;font-size:15px!important;margin-left:5px}.ls-ticket-no-legs{padding:28px;text-align:center;border:1px solid #5a456b;border-radius:13px;color:#b9aec5}.ls-ticket-cashout{width:calc(100% - 16px);margin:0 8px;min-height:48px;padding-left:16px;padding-right:16px;border:0;border-radius:8px;background:#d6ee46;color:#211b29;font:800 14px Rajdhani;cursor:pointer}.ls-ticket-cashout:disabled{opacity:.55}.ls-ticket-detail-error{color:#ff9eac;font-size:12px}.ls-ticket-unavailable{display:grid;place-items:center;gap:10px;min-height:420px;text-align:center;color:#b7abbf}.ls-ticket-unavailable .material-symbols-rounded{color:#d6ee46;font-size:34px}.ls-ticket-unavailable h1{margin:0;color:#fff;font-size:27px}.ls-ticket-unavailable p{max-width:420px;margin:0;color:#a99db2;font-size:12px}.ls-ticket-unavailable a{padding:11px 16px;border-radius:6px;background:#d6ee46;color:#211b29;text-decoration:none;font-size:12px;font-weight:800}.ls-ticket-loading,.ls-ticket-error{display:grid;place-items:center;gap:10px;min-height:420px;text-align:center;color:#b7abbf}.ls-ticket-loading i{width:20px;height:20px;border:2px solid #5a456b;border-top-color:#d6ee46;border-radius:50%;animation:lsTicketSpin .8s linear infinite}@keyframes lsTicketSpin{to{transform:rotate(360deg)}}.ls-ticket-error .material-symbols-rounded{color:#ff91a0;font-size:34px}.ls-ticket-error h1{margin:0;font-size:27px}.ls-ticket-error p{margin:0;color:#a99db2;font-size:12px}.ls-ticket-error a{padding:11px 16px;border-radius:6px;background:#d6ee46;color:#211b29;text-decoration:none;font-size:12px;font-weight:800}.ls-trophy-modal{position:fixed;inset:0;z-index:100;display:grid;place-items:center;padding:20px;background:rgba(7,4,14,.86);backdrop-filter:blur(3px)}.ls-trophy-close{position:fixed;top:16px;right:16px;z-index:3;display:grid;place-items:center;width:38px;height:38px;border:1px solid rgba(255,255,255,.45);border-radius:50%;background:#21182e;color:#fff;cursor:pointer}.ls-trophy-close .material-symbols-rounded{font-size:19px}.ls-trophy-modal-card{width:min(420px,100%);padding:24px 22px 20px;text-align:center;border:1px solid #d6ee46;border-radius:16px;background:linear-gradient(145deg,#39265c,#21182e);box-shadow:0 20px 60px rgba(0,0,0,.5)}.ls-trophy-kicker{margin:0;color:#d6ee46;font-size:11px;font-weight:800;letter-spacing:.17em}.ls-trophy-modal-card h2{margin:7px 0 0;font:900 36px/1 'Barlow Condensed',sans-serif;color:#fff}.ls-trophy-modal-card>strong{display:block;margin-top:8px;color:#57e5a2;font:800 23px 'Barlow Condensed',sans-serif}.ls-trophy-modal-card img{display:block;width:min(270px,76vw);height:260px;object-fit:contain;margin:5px auto 2px;filter:drop-shadow(0 12px 18px rgba(0,0,0,.4))}.ls-trophy-modal-card>button{min-width:150px}@media(max-width:620px){.ls-ticket-page{padding-bottom:104px}.ls-ticket-header{width:100%;margin-left:0;margin-right:0}body.ticket-details-route .mobile-bottom-nav{position:fixed!important;left:0;right:0;bottom:0;width:100%;z-index:80}.ls-ticket-summary-top{font-size:10px}.ls-ticket-summary-row>strong{font-size:22px}.ls-ticket-pick-box{grid-template-columns:1fr 1fr;padding-top:24px;box-sizing:border-box;width:100%;overflow:hidden}.ls-ticket-pick-box>div{border-right:0!important}.ls-ticket-pick-box>div:first-child{grid-column:1/-1;border-bottom:1px solid rgba(128,100,154,.45);padding-bottom:9px}.ls-ticket-pick-box>div:nth-child(2){border-right:0!important}.ls-ticket-pick-box>div:nth-child(3),.ls-ticket-pick-box>div:nth-child(4),.ls-ticket-pick-box>div:nth-child(5){border-top:1px solid rgba(128,100,154,.45);padding-top:8px}.ls-ticket-leg-card{padding:14px;box-sizing:border-box;width:100%;overflow:hidden}.ls-ticket-leg-top strong{font-size:16px}.ls-ticket-leg-card{margin-bottom:2px}.ls-ticket-legs{padding-bottom:8px}.ls-ticket-header h1{font-size:21px}.ls-ticket-header{min-height:70px;padding:0;margin-bottom:16px}.ls-ticket-win-banner{align-items:center}.ls-ticket-win-trophy{width:58px;height:58px;flex-basis:58px}.ls-ticket-win-trophy img{width:58px;height:58px}}`}</style>; }
